<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.broadband.product.mapper.SupportMapper">

    <select id="selectFaq" resultType="java.util.LinkedHashMap">
        SELECT id, category, question, answer FROM support_faq
        <where>
            <if test="category != null and category != ''">category = #{category}</if>
        </where>
        ORDER BY sort_order
    </select>

    <insert id="insertTicket">
        INSERT INTO support_ticket (id, customer_id, customer_name, type, content, contact, status, created_time)
        VALUES (#{id}, #{customerId}, #{customerName}, #{type}, #{content}, #{contact}, #{status}, #{createdTime})
    </insert>

    <select id="selectCustomerName" resultType="java.lang.String">
        SELECT name FROM customer WHERE id = #{id}
    </select>

</mapper>
