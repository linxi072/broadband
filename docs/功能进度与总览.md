# 宽带业务管理系统 · 功能进度与项目总览

> 文档版本：v1.10 ｜ 对应设计稿：v2.1 ｜ 更新日期：2026-09-15
> 合并来源：`功能进度说明书.md`（v1.10）+ `项目总览README.md`（v1.4）
> 目的：在一份文档中同时呈现「项目总览 / 产品定位 / 功能模块清单 / 功能进度状态 / 里程碑与版本 / 关键指标」，对齐研发、产品与运营预期。

---

## 一、项目总览

宽带业务管理系统是一套面向「家庭宽带办理与装维运营」的业务平台，覆盖 **客户端（微信小程序）**、**PC 后台管理系统（web-admin，Vue 3，已落地）** 与 **后端服务（Spring Boot 单体）** 三端。系统以「下单 → 小区可装性校验 → 智能派单 → 装维 SLA/赔付 → 套餐升级与流量监控」为主线，参考中国电信「美好家 / FTTR / 慢必赔」与中国移动「和家庭 / 承诺消费送宽带 / 流量提醒」等真实业务实践细化功能。

### 1.1 架构决策（已锁定）

- **单体架构**：后端为单一 Spring Boot 应用，**不引入 Spring Cloud / 微服务 / 服务注册发现 / 分布式网关**。
- **前端形态**：PC 后台为 Vue 3 单页应用，经 Nginx 反向代理直连后端 `:8082`。
- **模块上限**：后端业务模块上限 5 个，当前 4 个（community / install / product / system）。
- **配置策略**：优先使用 Spring Boot 原生（`application.yml` + 环境变量），外部配置中心（含 Apollo）暂不引入。

### 1.2 技术栈总览

| 端 | 技术选型 | 状态 |
|---|---|---|
| 微信小程序（客户端 / 师傅端） | 微信原生框架（WXML / WXSS / JS / app.json），utils/api.js 请求封装 | ✅ 客户端 20 页（含登录态）+ 师傅端 8 页（两独立工程） |
| PC 后台（web-admin） | Vue 3 + Element Plus（按需引入）+ Pinia + Vue Router + Vite | ✅ 已落地（22 业务页 / 22 路由） |
| 后端服务 | Spring Boot 3.2.1 + MyBatis-Plus（boot3 starter）+ JDK 21 + Maven，单体应用，端口 8082 | ✅ 4 模块 / 62 接口 |
| 数据库 | MySQL 8.4（25 张表 + 幂等初始化脚本，启动自动建表） | ✅ 已接入 |
| 权限 | Spring Security 6 + 自研 JWT（HS256，零三方依赖）+ RBAC | ✅ 已落地（5 角色 / 21 权限码） |
| 配置 | Spring Boot 原生（application.yml + 环境变量） | ✅ 采用 |

### 1.3 目录结构

```
宽带业务管理系统/
├── broadband-design/index.html   # 功能页面设计稿（HTML 线框，v2.1）
├── broadband-miniapp/            # 微信小程序客户端（蓝 #2563eb，20 页 + 登录态）
│   ├── app.js / app.json / app.wxss
│   ├── utils/api.js · auth.js    # 接口封装（Bearer 注入）+ 登录态校验
│   └── pages/                    # 20 个页面
├── broadband-worker/             # ★ 安装师傅端小程序（橙 #ea580c，8 页 + 登录态，独立工程）
│   ├── app.js / app.json / app.wxss / project.config.json / sitemap.json
│   ├── utils/api.js · auth.js    # 师傅端请求封装（Bearer 注入）
│   └── pages/                    # 8 个页面
├── web-admin/                    # ★ PC 运营后台（Vue 3 单页应用）
│   ├── vite.config.js            # 按需引入 Element Plus + /api 代理到 :8082
│   ├── .env.development / .env.production
│   └── src/
│       ├── api/                  # request.js（token 注入 + 401 处理）+ auth/business/system
│       ├── layout/               # 侧边栏（后端菜单树驱动）/ 顶栏 / 面包屑
│       ├── router/               # routes.js（22 条路由，meta.perm 与后端权限码同源）
│       ├── store/                # user（token + profile + menus）/ app
│       └── views/                # 25 个 .vue 页面
├── backend/                      # Spring Boot 后端（单体）
│   ├── pom.xml
│   ├── src/main/java/com/broadband/
│   │   ├── community/  install/  product/  system/   # 4 个业务模块 + common 公共层
│   ├── src/main/resources/
│   │   ├── application.yml        # 数据源/端口（环境变量注入，密码不落盘）
│   │   └── db/schema.sql + data.sql  # 25 张表（幂等，启动自动执行）
│   └── README.md / SLA.md / PKG_TRAFFIC.md / RBAC.md   # 模块级说明
└── docs/                         # 本目录：合并后的功能进度与总览
```

---

## 二、产品定位

- **业务主线**：下单 → 小区可装性校验 → 智能派单 → 装维 SLA/赔付 → 套餐升级与流量监控。
- **目标用户三角色**：① 终端客户（微信小程序自助办理）；② 安装师傅（独立小程序履约）；③ 运营/客服/财务（PC 后台管理）。
- **参考实践**：中国电信「美好家 / FTTR / 慢必赔」、中国移动「和家庭 / 承诺消费送宽带 / 流量提醒」。
- **关键业务能力**：融合套餐算价与补差升级、宽带可装性实时校验与需求登记、基于容量策略的智能派单、装维 SLA 双维度（时限类 + 速率类）评估与赔付、流量监控与续费提速、师傅端工单履约与数据隔离。
- **端到端链路**：`web-admin(Vue3) → Vite/Nginx → Spring Boot(:8082) → MySQL`（已 curl 实测）；小程序端经 `微信开发者工具 → 后端 :8082`（C 端登录态已接入真实 JWT 鉴权）。

---

## 三、功能模块清单

### 3.1 后端服务模块（4 个，上限 5）

每个模块遵循「纯 Java 引擎（model + engine，可 `javac`+`java` 独立运行）+ Spring 适配层（Mapper / Service / Controller）」的同构分层。核心算法模块保留 6 个可独立运行的 `*Demo`，无需 Maven / 数据库即可验证业务算法。

| 模块 | 职责 | 状态 | Java 文件 | 核心引擎 | REST 接口 |
|---|---|---|---|---|---|
| community 小区覆盖 | 可安装小区与需求登记 | ✅ 已完成 | 9 | CommunityChecker | 4（check / demand / communities GET,POST） |
| install 派单 + 装维 SLA | 派单调度 + 装维 SLA 与赔付 | ✅ 已完成 | 38 | CapacityPolicy / AdjacencyCluster / DispatchService / SlaEngine | 18（dispatch 2 / sla 5 / admin-work-order 5 / worker 6） |
| product 套餐 + 升级 + 流量 + 客户 | 套餐 / 升级 / 流量 / 客户 | ✅ 已完成 | 35 | PackageEngine / PackageUpgradeEngine / TrafficQueryEngine | 14（package 4 / order 1 / traffic 1 / admin 8） |
| system 权限与平台聚合 | RBAC 权限 + 后台聚合与监控 | ✅ 已完成（v1.2 新增 / v1.5 扩展） | 26 | JWT + Spring Security + JdbcTemplate 聚合 | 20（auth 5 / users 5 / roles 3 / menus 1 / logs 1 / platform 5） |
| common 公共层 | 主键生成 / 全局异常处理 | ✅ 已完成 | 2 | — | — |

**接口总数：62 个（GET 38 / POST 19 / PUT 5），全部读写真实 MySQL 数据。**

### 3.2 后端接口清单（62 个）

| 分组 | 方法与路径 |
|---|---|
| 认证 | `POST /api/auth/login`、`GET /api/auth/me`、`POST /api/auth/logout`、`POST /api/auth/miniapp-login`（微信 code 直登 / 演示验证码 1234 回退 → CUSTOMER 令牌）、`POST /api/auth/worker-login`（师傅手机号 → WORKER 令牌） |
| 小区（C 端开放） | `GET /api/community/check`、`POST /api/community/demand` |
| 派单 | `POST /api/dispatch/run`、`GET /api/dispatch/capacity` |
| SLA | `POST /api/sla/evaluate`、`POST /api/sla/evaluate-batch`、`GET /api/sla/board`、`GET /api/sla/rules`、`GET /api/sla/compensations` |
| 套餐（C 端开放） | `GET /api/package/detail`、`GET /api/package/list`、`GET /api/package/upgrade-options`、`POST /api/package/upgrade` |
| 流量（C 端开放） | `GET /api/traffic/usage` |
| 订单（C 端开放） | `GET /api/order/my?customerId=`（按客户查本人订单） |
| 后台 · 小区 | `GET /api/admin/communities`、`POST /api/admin/communities` |
| 后台 · 工单与师傅 | `GET /api/admin/work-orders`、`GET /api/admin/work-orders/{id}`、`POST /api/admin/work-orders/reset`、`GET /api/admin/workers`、`POST /api/admin/worker-capacity` |
| 后台 · 客户与套餐 | `GET /api/admin/customers`、`GET /api/admin/packages`、`POST /api/admin/packages` |
| 后台 · 升级 / 评价 / 销售 / 财务 | `GET /api/admin/upgrade-orders`、`GET /api/admin/reviews`、`PUT /api/admin/reviews/{id}/close`、`GET /api/admin/sales/report`、`GET /api/admin/finance/report` |
| 后台 · 平台聚合 | `GET /api/admin/dashboard/stats`、`GET /api/admin/dashboard/recent-orders`、`GET /api/admin/orders`、`GET /api/admin/traffic/overview`、`GET /api/admin/monitor/overview` |
| 系统 · 用户 | `GET /api/system/users`、`POST /api/system/users`、`PUT /api/system/users/{id}/status`、`PUT /api/system/users/{id}/roles`、`PUT /api/system/users/{id}/password` |
| 系统 · 角色 | `GET /api/system/roles`、`GET /api/system/roles/{id}/menus`、`PUT /api/system/roles/{id}/menus` |
| 系统 · 菜单与日志 | `GET /api/system/menus/tree`、`GET /api/system/logs` |
| 师傅端（数据按登录师傅隔离） | `GET /api/worker/work-orders`、`GET /api/worker/work-orders/{id}`、`GET /api/worker/summary`、`GET /api/worker/capacity`、`GET /api/worker/schedule`、`POST /api/worker/work-orders/{id}/complete` |

### 3.3 数据与安全基建

| 项 | 状态 | 说明 |
|---|---|---|
| 数据库接入（MySQL 8.4） | ✅ 已完成 | **25 张表** + 幂等 `schema.sql` / `data.sql`，启动自动初始化；v1.5 增列 `customer.openid`、`worker.phone` |
| RBAC 权限模型 | ✅ 已完成 | `sys_user` / `sys_role` / `sys_menu` / `sys_user_role` / `sys_role_menu` / `sys_oper_log`；5 角色 · 26 菜单 · 21 权限码 |
| JWT 鉴权 | ✅ 已完成 | Spring Security 6 + 自研 HS256 JWT（零三方依赖）；角色权限**每请求实时查库**，改授权即时生效；v1.5 扩为**三类令牌**：SYS（后台 RBAC）/ CUSTOMER（客户端）/ WORKER（师傅端） |
| C 端鉴权开关 | ✅ 已完成 | `app.security.protect-client-api: true`（v1.5 起 C 端强制校验 JWT，一处开关可回退） |
| 微信登录双路径 | ✅ 已完成 | 配 `WECHAT_APPID`/`WECHAT_SECRET` → `code2Session` 换 openid 绑定客户；未配 → 演示码 `1234` 回退 |
| 回归单测 | ✅ 已完成 | `JwtUtilTest` 7 条用例（数字声明解析 / 篡改签名 / 篡改载荷 / 过期 / 密钥错配 / 格式非法） |
| 配置中心 | ⬜ 不引入 | 按架构决策走 Spring Boot 原生 yml + 环境变量 |

**默认账号**（初始密码由 `RbacInitializer` 以 BCrypt 写入，幂等不覆盖用户改过的密码）

| 账号 | 密码 | 角色 | 可见菜单 | 权限码 |
|---|---|---|---|---|
| admin | admin123 | ADMIN 超级管理员 | 26 | 21 |
| liuwei | liuwei123 | OPERATOR 运营专员 | 19 | 15 |
| zhaomin | zhaomin123 | FINANCE 财务 | 4 | 4 |
| wangfang | wangfang123 | CS 客服 | 4 | 4 |

> ⚠️ 生产环境务必首次登录后立即改密，并通过环境变量覆盖 `app.jwt.secret`。

### 3.4 三端功能清单

**客户端小程序（微信原生）· `broadband-miniapp`（蓝 `#2563eb`，20 页 + 登录态）**

| 分组 | 页面 |
|---|---|
| 核心 | 首页 `pages/index`、服务聚合 `pages/service`、个人中心 `pages/profile`（含登录门禁）、设置 `pages/settings`、登录 `pages/login` |
| 套餐与流量 | 套餐详情 `pages/package/detail`、套餐列表 `pages/package/list`、套餐升级 `pages/package/upgrade` + `pages/upgrade`、流量监控 `pages/traffic` |
| 下单与安装 | 可装小区查询 `pages/community/query`、安装地址 `pages/order/address`、订单确认 `pages/order/confirm`、订单列表 `pages/order/list`、订单详情 `pages/order/detail`、地址管理 `pages/address`、移机 `pages/move` |
| 服务与报修 | 自助服务 `pages/self`（续费/提速/报修/发票）、报修评价 `pages/review`、智慧家庭 `pages/smart` |

**安装师傅端小程序（微信原生）· `broadband-worker`（橙 `#ea580c`，8 页 + 登录态，独立工程）**

| 分组 | 页面 |
|---|---|
| 工作台 | 首页 `pages/index`（今日工单概览）、个人中心 `pages/profile`（含登录门禁）、登录 `pages/login` |
| 工单履约 | 我的工单 `pages/order` + 详情 `pages/order-detail`、完工确认/测速 `pages/confirm` |
| 调度支撑 | 排班与容量 `pages/capacity`（时段热力 + 个人容量）、工单路线 `pages/schedule` |

**PC 后台管理系统（web-admin，Vue 3 + Element Plus，22 业务页 / 22 路由，靛 `#4f46e5`）**

| 分组 | 页面 |
|---|---|
| 经营总览 | 数据看板 `/dashboard` |
| 业务办理 | 订单 `/order`、客户 `/customer`（含分层与升档抽屉）、套餐 `/package`、套餐编辑 `/package/edit`、套餐升级 `/package/upgrade`、小区 `/community`、小区编辑 `/community/edit` |
| 装维履约 | 工单池 `/workorder/pool`、派单调度 `/workorder/dispatch`、容量配置 `/workorder/capacity`、调度规则 `/workorder/rules`、装维 SLA 与赔付 `/sla` |
| 运营支撑 | 流量监控 `/traffic`、投诉与评价 `/review`、销售管理 `/sales`、财务管理 `/finance` |
| 系统管理 | 用户 `/system/user`、角色 `/system/role`、菜单 `/system/menu`、操作日志 `/system/log`、性能监控 `/monitor` |

---

## 四、功能进度状态表

### 状态图例

| 标记 | 含义 |
|---|---|
| ✅ 已完成 | 代码已落地，可编译或可运行验证 |
| 🟡 部分完成 | 骨架或设计已出，核心逻辑待补全/联调 |
| ⬜ 规划中 | 仅设计稿，尚未编码 |

### 4.1 客户端小程序进度

| 模块 | 功能 | 状态 | 进度 |
|---|---|---|---|
| 客户端 | 首页（Banner + 快捷入口） | ✅ 已完成 | 100% |
| 客户端 | 服务聚合页（九宫格导航） | ✅ 已完成 | 100% |
| 客户端 | 融合套餐详情（算价 + 立即办理） | ✅ 已完成 | 100% |
| 客户端 | 套餐列表 | ✅ 已完成 | 100% |
| 客户端 | 套餐升级（升档/加购/补差） | ✅ 已完成 | 100% |
| 客户端 | 流量监控（7 日趋势） | ✅ 已完成 | 100% |
| 客户端 | 可安装小区查询 | ✅ 已完成 | 100% |
| 客户端 | 安装地址填写（联动校验） | ✅ 已完成 | 100% |
| 客户端 | 新装宽带 / 移机 / 续费提速 | ✅ 已完成 | 100% |
| 客户端 | 报修 / 自助服务 | ✅ 已完成 | 100% |
| 客户端 | 智慧家庭 | ✅ 已完成 | 100% |
| 客户端 | 评价投诉 | ✅ 已完成 | 100% |
| 客户端 | 我的订单（列表 + 详情） | ✅ 已完成 | 100% |
| 客户端 | 订单确认 / 地址管理 | ✅ 已完成 | 100% |
| 客户端 | 个人中心 / 设置 / 登录 | ✅ 已完成 | 100% |
| 客户端 | 统一请求封装（Bearer + 401 跳登录 + 不静默吞错） | ✅ 已完成 | 100% |

### 4.2 师傅端小程序进度

| 模块 | 功能 | 状态 | 进度 |
|---|---|---|---|
| 师傅端 | 师傅首页（今日工单概览） | ✅ 已完成 | 100% |
| 师傅端 | 我的工单（列表 + 详情，按 token 隔离） | ✅ 已完成 | 100% |
| 师傅端 | 完工确认（测速/签名/服务项，置 DONE） | ✅ 已完成 | 100% |
| 师傅端 | 排班与容量看板（本人） | ✅ 已完成 | 100% |
| 师傅端 | 工单路线（7 天排班 + 当日顺序） | ✅ 已完成 | 100% |
| 师傅端 | 测速报告（落库 + SLA 判定） | ✅ 已完成 | 100% |
| 师傅端 | 星级评价（查看客户评价） | ✅ 已完成 | 100% |
| 师傅端 | 个人中心 / 登录（worker-login 真实令牌） | ✅ 已完成 | 100% |
| 师傅端 | 统一请求封装（Bearer + 401 跳登录） | ✅ 已完成 | 100% |
| 师傅端 | 视觉验收（无浏览器，需开发者工具截图） | ⬜ 规划中 | 0% |

### 4.3 PC 后台进度

| 模块 | 功能 | 状态 | 进度 |
|---|---|---|---|
| 后台 | 数据看板 dashboard | ✅ 已完成 | 100% |
| 后台 | 订单管理 / 客户管理 / 套餐管理 / 套餐升级 | ✅ 已完成 | 100% |
| 后台 | 小区管理 / 工单池 / 派单调度 / 容量配置 / 调度规则 | ✅ 已完成 | 100% |
| 后台 | 装维 SLA 与赔付 | ✅ 已完成 | 100% |
| 后台 | 流量监控 / 投诉与评价 / 销售 / 财务 | ✅ 已完成 | 100% |
| 后台 | 权限管理（用户/角色/菜单/日志） | ✅ 已完成 | 100% |
| 后台 | 性能监控 monitor | ✅ 已完成 | 100% |
| 后台 | 登录（JWT + 路由守卫 + 菜单权限过滤） | ✅ 已完成 | 100% |
| 后台 | UI 精致化（状态标签/空态/骨架屏/全局视觉，M14 收官） | ✅ 已完成 | 100% |

### 4.4 后端服务进度

| 模块 | 功能 | 状态 | 进度 |
|---|---|---|---|
| 后端 | community 小区覆盖（4 接口） | ✅ 已完成 | 100% |
| 后端 | install 派单 + 装维 SLA（18 接口） | ✅ 已完成 | 100% |
| 后端 | product 套餐/升级/流量/客户（14 接口） | ✅ 已完成 | 100% |
| 后端 | system 权限与平台聚合（20 接口） | ✅ 已完成 | 100% |
| 后端 | common 公共层 | ✅ 已完成 | 100% |
| 后端 | 微信真实支付（PayService seam，当前模拟） | ⬜ 规划中（接 SDK 即可） | 0% |
| 后端 | 监控与配置治理（metrics/日志聚合/告警/多环境） | ⬜ 规划中（M12） | 0% |

---

## 五、里程碑与版本记录

### 5.1 里程碑

| 里程碑 | 状态 | 产出 |
|---|---|---|
| M1 需求与设计稿 | ✅ | broadband-design/index.html v2.1 |
| M2 后端核心算法 + 接口 | ✅ | 3 模块 / 13 接口 / 6 个可运行 Demo |
| M3 客户端小程序骨架 | ✅ | 20 页（含登录页）+ api 封装 + 登录态骨架 |
| M4 数据库接入与联调 | ✅ | MySQL 17 表 + 幂等初始化 + 13 接口真实数据 + 端到端验证 |
| M5 PC 后台落地 | ✅ 已完成 | web-admin 工程 + 22 业务页 + 22 路由 + 全量对接真实接口 |
| M6 安全体系 | ✅ 已完成 | Spring Security 6 + JWT + RBAC（5 角色 / 21 权限码）+ 鉴权矩阵实测 + 7 条单测 |
| M7 小程序补页 + 登录态 | ✅ 已完成 | 客户端 20 页 + 师傅端 8 页（两独立工程）+ 登录态（真实 JWT 鉴权） |
| M8 缺陷修复（小程序契约/登录） | ✅ 已完成 | 修复 4 个真实缺陷：404/越权端点、静默吞错、mock 登录；新增 4 个接口（45→49） |
| M9 双端登录态收口 | ✅ 已完成 | C 端强制鉴权 + CUSTOMER 令牌；师傅端 `worker-login` + WORKER 令牌；微信 `code2Session` 双路径；接口 49→50 |
| M10 师傅端数据绑定与隔离 | ✅ 已完成 | 新增 `WorkerController`（6 接口）+ 服务端按 token 隔离；4 个 mock 页面全绑定；接口 50→56 |
| M10b 师傅端视觉验收 | ⬜ | 无浏览器，需微信开发者工具导入后截图验收 |
| M11 业务闭环联调 | ✅ 已完成 | 打通下单→支付→派单→装机→SLA 赔付→评价全链路；接口 56→62；端到端 36 项断言全绿 |
| M12 监控与配置治理 | ⬜ | metrics / 日志聚合 / 告警 + 多环境 profile 隔离 |
| M13 CI/CD 与上线 | ⬜ | 流水线 + 生产 Nginx 部署 + 文档终版 + 复盘 |
| M14 三端 UI 优化 | ✅ 已完成 | 小程序双端设计系统 v2.0 + 客户端 11 骨架页重写 + 师傅端 8 页全量重写 + web-admin 精致化收官（`npm run build` 通过） |

### 5.2 版本变更记录

- **v1.1**：Phase 2「数据持久化与真实联调」—— 后端接入 MySQL，13 个 REST 接口全部读写真实数据。
- **v1.2**：Phase 3「PC 后台 + 安全体系」—— 新建 `web-admin`（22 业务页 + 登录），接口 13→45、表 17→25；落地 Spring Security 6 + JWT + RBAC（21 权限码 / 5 角色）。
- **v1.3**：小程序补页（M7）—— 客户端 7→20 页，新建师傅端 8 页（独立工程）；统一请求封装注入 Bearer token。
- **v1.4**：修复小程序真实缺陷（M7 收尾）—— 登录态由 mock 升级为真实 JWT；修复 4 个契约/逻辑缺陷；接口 45→49。
- **v1.5**：M9「双端登录态正式收口」—— C 端强制鉴权 + CUSTOMER 令牌；师傅端 `worker-login` + WORKER 令牌；微信 `code2Session` 双路径；接口 49→50。
- **v1.6**：M9「师傅端数据全绑定 + 仅看本人工单」—— 新增 `WorkerController`（6 接口）；服务端按 token 隔离；接口 50→56；13 项验证全绿。
- **v1.7**：M10 收尾「凭证落地 + 文档对齐」—— 客户端/师傅端写入真实 AppID `wxcc344ad7f4f849d4`；师傅端接口口径修正为 `/api/worker/*`；新增外部凭证清单。
- **v1.8**：M11「业务闭环联调」—— 打通下单→支付→派单→装机→SLA 赔付→评价；PayService 支付 seam；接口 56→62；`scripts/loop_test.py` 端到端 36 项断言全绿。
- **v1.9**：三端 UI 优化（小程序双端）—— 统一设计系统 v2.0（token 体系 + 全局通用组件类）；客户端 11 骨架页重写；师傅端 8 页全量重写；web-admin 精致化延至第二批。
- **v1.10**（本次）：M14 第二批 —— web-admin 精致化收官：统一状态标签体系（`status.js` + `StatusTag`）、精致空态 `EmptyState` 替换 26 处裸空态、仪表盘骨架屏、`ChartBox` 图表空态、`index.css` 全局视觉打磨；`npm run build` 通过（2400+ 模块转换，零错误）。

---

## 六、关键指标统计

### 6.1 总体进度统计

| 维度 | 已完成 | 规划 | 覆盖率 |
|---|---|---|---|
| 后端业务模块 | 4（+1 公共层） | 上限 5 | 100% |
| 后端 REST 接口 | 62（GET 38 / POST 19 / PUT 5，全部连库可用） | 0 | 100% |
| 数据库表 | 25 张 + 种子数据 | 0 | 100% |
| 安全 RBAC | 5 角色 / 26 菜单 / 21 权限码 | 0 | 100%（Phase 3 达成） |
| PC 后台模块 | 22 个业务页 / 22 条路由 | 0（设计稿 16 模块全覆盖） | 100%（Phase 3 达成） |
| 客户端小程序页 | 20（含登录页） | 0（设计稿 20 页全覆盖） | 100%（M7 达成） |
| 师傅端小程序页 | 8（含登录页） | 0（设计稿 8 页全覆盖） | 100%（M7 达成） |

### 6.2 端到端验证

- 业务闭环联调（M11）：`scripts/loop_test.py` **36 项断言全绿**（A 正常双达标 / B 速率不达标现金赔付 / C 超时慢必赔 vouchers）。
- 师傅端隔离验证（M9/M10）：**13 项验证全绿**（含 5 项隔离/越权用例）。
- 安全鉴权矩阵：无 token → 401、越权 → 403、授权内 → 200，实测通过。
- 回归单测：`JwtUtilTest` 7 条用例全绿。

### 6.3 外部凭证与环境变量清单

| 凭证 / 变量 | 位置 | 状态 | 说明 |
|---|---|---|---|
| 微信小程序 AppID | `broadband-miniapp/project.config.json` · `broadband-worker/project.config.json` · 后端 `app.wechat.miniapp.appid` | ✅ 已配置 | `wxcc344ad7f4f849d4`；两端共用，提审前确认是否需拆分 |
| 微信小程序 AppSecret | 环境变量 `WECHAT_SECRET` | ⬜ 待提供 | 缺失即自动回退演示码 1234；注入后后端自动走 `code2Session` |
| JWT 密钥 | `app.jwt.secret`（建议环境变量覆盖） | ✅ 已配置（默认） | 生产环境必须覆盖默认值并首次登录后改密 |
| MySQL 连接 | `DB_HOST` / `DB_PORT` / `DB_NAME` / `DB_USERNAME` / `DB_PASSWORD` | ✅ 本地已通 | 生产库凭据走环境变量，不入库 |
| 客户端鉴权开关 | `app.security.protect-client-api` | ✅ `true` | v1.5 起 C 端接口强制鉴权；如需临时开放置 `false` 即可 |

### 6.4 已知缺口（剩余工作）

1. 微信 AppSecret 未注入，登录当前仍走演示码 `1234` 回退路径（AppID 已就位，仅需导出环境变量 `WECHAT_SECRET` 即可切到 `code2Session`）。
2. 师傅端页面视觉验收（无浏览器，需微信开发者工具导入后截图，M10b）。
3. 真实微信支付（PayService 已留 seam，接 SDK 即可，当前为模拟支付）。
4. 监控与配置治理（M12）、CI/CD 与上线（M13）。

### 6.5 历史真实缺陷（可追溯）

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 登录成功、token 正确但所有受保护接口一律 401 | `JwtUtil` 数字判定误写导致 `iat`/`exp` 解析失败，过滤器静默吞异常 | 改为 `Character.isDigit`；过滤器补 WARN 日志；新增 7 条单测 |
| 2 | `GET /api/admin/orders` 返回 500 | 联表 `SELECT id` 未加表别名 → 列歧义 | 全部列加 `o.` 前缀 |
| 3 | `SlaEvaluation.summary` 输出 `[null]` | 主键在评估之后才生成 | 主键改为评估前生成 |
| 4 | 客户端「套餐列表」无数据 | `api.listPackages()` 请求 `/api/package/list` 无此端点 → 404 | 后端新增 `GET /api/package/list` 及 Mapper |
| 5 | 客户端「我的订单」空/被拦截 | `api.getOrders()` 请求 `/api/admin/orders`（越权 → 403） | 后端新增 `GET /api/order/my`；客户端改调 |
| 6 | 小程序登录形同虚设 | `login()` 本地返回假 token，未与后端交互 | 后端新增 `POST /api/auth/miniapp-login` 签发真实 JWT |
| 7 | 任何接口报错都被当成成功 | `request()` 仅 `fail` 时 reject，`statusCode>=400` 仍 resolve | 改为非 2xx 一律 reject（带 message） |
| 8 | C 端令牌「能签发却用不了」恒 401 | `JwtAuthFilter` 只认 `sys_user` 表，CUSTOMER 令牌不在后台用户表 | 过滤器按 `dept` 分支建 `CustomerPrincipal`/`WorkerPrincipal` |
| 9 | 师傅端登录形同虚设 | `workerLogin()` 本地返回假 token | 后端新增 `POST /api/auth/worker-login` 签发 WORKER 令牌 |
| 10 | 师傅端能看到全部工单 | 复用后台端点无 worker 过滤，且客户端可传 `?workerId=` | 新增 `/api/worker/*` 服务端按 token 强制过滤 |
| 11 | 师傅端 4 页硬编码假数据 | 首页统计/容量/排班/完工均写死，提交只是 `setTimeout` | 新增 worker 真实接口，4 页改调，`work_order` 增 5 列落库 |

> 第 1–3 条于 RBAC 阶段修复；第 4–7 条于 M8 修复；第 8–9 条于 M9 修复；第 10–11 条于 M10 修复。第 7 条与第 1 条同源（静默吞错系统性风险）；第 8 条与第 6 条同源（"看起来完成"的伪实现）。

---

> 配套文档：本合并文档取代原 `功能进度说明书.md` 与 `项目总览README.md`。接口明细、数据模型与分层设计详见《系统架构说明书》；环境、部署与故障排查详见《运维操作说明书》；路线图与验收标准详见《系统规划说明书》。
