# 宽带业务管理系统 · 后续项目迭代计划

> 文档版本：V1.0（首版规划）｜ 编制日期：2026-09-16
> 基线版本：**V1.13**（已交付：故障报修全流程 / 数据字典 / 参数配置 / 运营·财务 PC 页实时化）
> 适用范围：微信小程序（客户端 + 师傅端）+ Vue3 PC 后台 + Spring Boot 3.2 后端
> 关联文档：《项目进度与开发计划》(V1.13)、《迭代与需求》(V1.9+)、《事项分配》(T-01~T-11)
> 说明：本文是《事项分配》的**规划深化版**，将 T-01~T-11、G1~G11 重组为 4 个可执行迭代（v1.14 / v1.15 / V2.0 / M15），给出范围、用户故事、接口、验收、工作量与关键路径，供排期与补员后直接认领。

---

## 一、规划基线（从哪来）

### 1.1 本轮（V1.13）已交付
- 故障报修全流程：小程序 3 页（repair/list/detail）+ 后端 `RepairController` + 复用派单/SLA/评价链路。
- 数据字典：`sys_dict_type/data` + 后台 CRUD（权限码 `system:dict`）+ 免鉴权 `/api/dict/public/{type}`。
- 参数配置：`sys_config` + 后台 CRUD（权限码 `system:config`）+ 免鉴权 `/api/config/public/{key}`。
- 运营 / 财务 PC 页全量接入实时数据；DictConfigInitializer 幂等 seed（菜单/字典/配置）。
- **本轮新增深度优化（T-04 收尾）**：三端关键指标下钻已落地并验证 ——
  - SLA 超时热力图 → 点击单元格打开「超时明细抽屉」（`/api/admin/sla/overtime-detail`）；
  - 套餐营销转化漏斗 → 点击阶段打开「订单明细抽屉」（`/api/admin/product/funnel-detail`）；
  - 客户 360 生命周期标签 → 点击「下钻」切换至触达记录 Tab。
  - 验证：后端冒烟全绿（overtime-detail 命中 DRILL-1、5 个漏斗阶段均 200、热力图 7×24）；前端 `npm run build` 通过。

### 1.2 待清算项（须先收口再进 v1.14）
| ID | 项 | 现状 | 处理建议 |
|---|---|---|---|
| — | T-04 下钻代码本地提交 | 已改未 commit（8 文件） | 本文完成后随迭代计划文档一并本地提交 |
| — | 本地 commit `f421460` 未 push | 沙箱 SSH 受限，push 被拦 | 需用户在本地 `git push` 或提供 GitHub Token |
| G5/T-01 | 师傅端视觉验收 | 8 页已接真实接口，无截图 | 见迭代一 |
| G4/T-03 | 微信支付真集成 | Mock 桩，阻塞于凭证 | 见迭代二（阻塞项） |

---

## 二、迭代总路线图

```
V1.13 核心自助 + 平台治理 ✅
  └─ 迭代一 · v1.14 运营与留存（P0/P1，约 2~3 周）
       ① 账户账单中心补齐 ② 积分成长体系 ③ 优惠活动专区 ④ 在线客服/帮助中心
       ⑤ 师傅端视觉验收(T-01) ⑥ 安全治理(T-02)启动评审
        └─ 迭代二 · v1.15 数据深化与交易真闭环（P1，约 2~3 周）
             ⑦ 微信支付真集成(T-03，凭证到位即排首) ⑧ 报表下钻标准化 ⑨ 后台扩展深化(T-04 剩余)
             ⑩ 配置动态刷新(T-05，可选) ⑪ 压测长稳(T-06，可选)
              └─ 迭代三 · V2.0 智能化与多端（P2/P3，约 3~4 周）
                   ⑫ 多住宅切换(T-07) ⑬ 高频入口智能推荐 ⑭ 宽带暂停/恢复
                   ⑮ 国际化 i18n(G11/T-07)
                    └─ 里程碑 · M15 上线保障（上线前，约 1~2 周）
                         ⑯ CI/CD 流水线 + 生产 Nginx(T-09) ⑰ 文档终版 + 复盘(T-10)
                         ⑱ 安全治理(T-02)硬性门禁闭环
```

> 排期口径：以「单人承接（PM 兼后端）」为悲观基线；若补员（前端/测试/运维）则各迭代可并行压缩。所有日期为相对估算，建议以周为单位滚动确认。

---

## 三、迭代一 · v1.14 运营与留存

> **目标**：把客户端从「下单工具」升级为「随身营业厅」，补齐 v1.13 未覆盖的核心自助（账户账单），并引入运营留存三件套（积分/活动/客服）；同时启动上线前的安全与视觉验收。
> **范围**：客户端小程序 + web-admin（活动/客服后台）+ 后端（points/promotion/support/account 接口）+ 师傅端验收 + 安全评审。
> **对应事项**：T-08（主）、T-01、T-02（启动）、G10 部分。

### 3.1 用户故事与交付清单

| 编号 | 故事 | 前端 | 后端 | 验收标准 |
|---|---|---|---|---|
| US-1.1 | 账户与账单中心 | 客户端新增 `account/account`、`account/bills`；「我的」页加余额/账单/发票入口 | `GET /api/account/summary`、`GET /api/account/bills`（聚合 biz_order+退款+发票） | 余额/本月消费/合约到期展示正确；账单明细与订单一致；发票打通 V1.11 占位 |
| US-1.2 | 积分成长体系 | `points/points`（签到/任务中心/明细）、`points/mall`（兑换提速包/体验券/实物） | `points` 模块：`balance`/`sign`/`tasks`/`mall`/`redeem`（兑换生成 biz_order 或优惠券） | 签到得积分闭环；任务完成计积分；商城兑换生成对应单据 |
| US-1.3 | 优惠活动专区 | 新增「发现」Tab 或首页活动聚合卡 + 活动详情/一键办理 | `GET /api/promotions`（列表/详情）；下单校验优惠（扩展 PackageUpgradeEngine） | 活动流展示；活动详情可达；办理时优惠正确生效 |
| US-1.4 | 在线客服 / 帮助中心 | `support/support`：FAQ 检索 + 自助排障 + 工单式咨询 | `GET /api/support/faq`、`POST /api/support/ticket`；与报修/订单工单互链 | FAQ 可检索；咨询留言可达；工单可跟踪 |
| US-1.5 | 师傅端视觉验收 | 微信开发者工具导入 `broadband-worker/`，8 页截图 | —（无新接口） | 输出《师傅端视觉验收报告》，偏差 ≤ 3 项（T-01） |
| US-1.6 | 安全治理评审启动 | — | 梳理 `JWT_SECRET` 环境变量覆盖点 + 首登改密逻辑 | 产出安全检查清单初稿，列入 M15 硬性门禁（T-02） |

### 3.2 退出标准（DoD）
- [ ] 客户端 4 项自助（账户/积分/活动/客服）可演示闭环，SourceTag 标注「实时数据」。
- [ ] 积分/活动/客服后台可配置（web-admin 运营页）。
- [ ] 师傅端 8 页验收报告归档；偏差项登记 backlog。
- [ ] 安全检查清单 v1 评审通过，T-02 进入上线前必做队列。

### 3.3 工作量与依赖
- 悲观（单人）：前端 8~10pd + 后端 6~8pd + 验收 2pd ≈ **16~20pd（2~3 周）**。
- 关键依赖：师傅端 AppID（占位 `wxWORKER_APPID_TODO`）提审前须注册注入（T-01 阻塞）。

---

## 四、迭代二 · v1.15 数据深化与交易真闭环

> **目标**：关闭最大外部阻塞（微信支付），把数据看板从「能看」做到「能下钻能导出」，并深化后台运营分析能力；补齐工程化长稳。
> **范围**：后端（微信支付 APIv3 / 报表聚合 / 配置热刷新）、web-admin（报表下钻标准化 / 后台扩展深化）、测试（压测长稳）。
> **对应事项**：T-03（主，阻塞）、T-04（剩余）、T-05、T-06、G10 剩余。

### 4.1 用户故事与交付清单

| 编号 | 故事 | 前端 | 后端 | 验收标准 |
|---|---|---|---|---|
| US-2.1 | 微信支付真集成 | 客户端/后台支付回调对接（替换 Mock） | `PayService` → 真实 APIv3（下单/回调验签/退款/对账单），R4 `refund_no` 对接真实单号 | 真实小额支付→回调→订单状态机→退款全链路跑通（T-03） |
| US-2.2 | 报表下钻标准化 | 销售/财务/运营看板统一「图表点击→明细抽屉」交互（复用 ChartBox `chart-click`） | `sales/report` / `finance/report` 增补 `*detail` 明细接口 | 所有关键指标一致可下钻；交互与 SLA/营销看板一致 |
| US-2.3 | 后台扩展深化 | 客户 360 生命周期/触达、营销漏斗、SLA 超时热力/赔付趋势三页深化 | 配合 US-2.2 聚合接口 | 三页实时化 + 可下钻（T-04 最终验收） |
| US-2.4 | 配置动态刷新 | — | `sys_config` / 容量规则 / SLA 阈值变更后热生效（`@RefreshScope` 或刷新端点） | 改配置无需重启即生效（T-05） |
| US-2.5 | 压测长稳 | — | 提供 JMeter 长稳脚本（≥2h） | 长稳期无内存泄漏、错误率 < 0.1%（T-06） |

### 4.2 退出标准（DoD）
- [ ] T-03 支付闭环真实跑通（凭证到位前提下）；否则保留 Mock 并标注阻塞。
- [x] 全部看板关键指标可下钻（图表交互统一）—— US-2.2 已交付（2026-09-18）。
- [ ] T-04 三页深化验收通过。
- [ ] T-05/T-06 至少其一落地（建议 T-05 优先，收益高）。

### 4.3 工作量与依赖
- 悲观（单人）：US-2.1 4~6pd（凭证到位后）、US-2.2/2.3 6~8pd、US-2.4 2~3pd、US-2.5 2pd ≈ **14~19pd（2~3 周）**。
- **关键路径阻塞**：T-03 依赖商户号 / APIv3 密钥 / 平台证书。凭证到位即排首位；未到位则先行 US-2.2~2.5，T-03 单列跟踪。

### 4.4 数据智能模块（V1.15 已交付 ✅）

> 作为「数据深化」主题的先行交付，独立于 US-2.1~2.5（支付/报表下钻/配置热刷新/压测），已完整落地后端 + web-admin 前端。

| 能力 | 后端接口 | 前端页面 | 状态 |
|---|---|---|---|
| 客户分群（6 类生命周期） | `GET /api/intelligence/segments` | 数据智能页-分群饼图+占比表 | ✅ |
| 流失预警（风险分+归因） | `GET /api/intelligence/churn` | 数据智能页-风险柱图+高危表 | ✅ |
| 营销自动化规则引擎 | `GET /api/intelligence/campaigns`、`POST /api/intelligence/auto-trigger` | 数据智能页-规则表+一键触发 | ✅ |

- 数据模型：新增 `mkt_campaign` / `mkt_campaign_exec` 2 表；seed 3 条规则（流失挽留/临期续约/高价值提速）。
- 权限：`intelligence:view` 菜单 M150，已同源授权 ADMIN/OPERATOR。
- 冒烟：4 接口全过，auto-trigger 实测 `matched=2 / granted=1 / pushed=1`，30 天去重生效。
- **US-2.2 报表下钻标准化（2026-09-18 已交付 ✅）**：复用本模块聚合口径（`CUSTOMER_BASE_CTE` + `SEGMENT_CASE` 两常量统一 segments/churn/targetCustomers/segment-customers），新增 3 个下钻接口（`/intelligence/segment-customers`、`/admin/sales/report/detail`、`/admin/finance/report/detail`）；web-admin 新增 `components/DetailDrawer.vue` 统一 chart-click→抽屉明细，sales/finance/intelligence 三页已接入。11 项冒烟全过。
- 待后续衔接：US-2.3 客户 360 生命周期可引用分群结果。

- **US-2.4 配置动态刷新（2026-09-20 已交付 ✅）**：
  - 容量规则外置：`CapacityPolicy` 改为读 `ConfigCacheService`（静态 `bind` 桥接 `InstallConfigBridge`，纯算法类保持无 Spring 依赖，静态常量兜底）；相邻/非相邻默认上限·下限、相邻判定距离均经 `sys_config` 配置（`dispatch.default.adjacent.cap/min`、`dispatch.default.non.adjacent.cap/min`、`dispatch.adjacent.distance.meters`），改值经 `PUT /api/system/config/{key}` 或 `POST /api/system/config/refresh` 免重启生效。
  - SLA 阈值后台可改：新增 `SlaAdminController`（`GET /api/admin/sla/rules`、`PUT /api/admin/sla/rule/{id}`，权限码 `sla:config`，菜单 M151 三处同源授权）；`sla_rule` 表实时读 DB，阈值变更天然热生效。
  - 冒烟：配置 `4→9→4` 经缓存消费者（`/api/config/public/...` 读缓存）验证热刷新；SLA 规则 `graceMinutes 0→123→0` 持久化验证；M151 菜单与 ADMIN 授权确认。commit a88e677。

---

## 五、迭代三 · V2.0 智能化与多端

> **目标**：体验与能力扩展——多住宅统一管、首页智能推荐、宽带暂停/恢复、中英双语。
> **范围**：全栈（客户端/后台/后端新接口）。
> **对应事项**：T-07、T-11、G11。

### 5.1 用户故事与交付清单

| 编号 | 故事 | 前端 | 后端 | 验收标准 |
|---|---|---|---|---|
| US-3.1 | 多住宅切换 | `address` 升级「我的住户」，新增/切换住户；账单/工单/套餐按住户隔离 | 账号下挂多 `household_id`；数据按住户维度隔离 | 多户隔离正确；切换后数据随之过滤 |
| US-3.2 | 高频入口智能推荐 | 首页快捷办理基于近 30 天行为动态排序 | `GET /api/recommend/shortcuts`（行为聚合） | 高频功能入口动态靠前；三步办结率提升 |
| US-3.3 | 宽带暂停 / 恢复 | `self` 页开关 + 暂停计费规则提示 | `POST /api/broadband/pause`、`POST /api/broadband/resume` | 暂停期间计费规则生效；恢复后业务正常 |
| US-3.4 | 国际化 i18n | 客户端/后台文案抽取为 i18n 资源，中英切换 | 文案资源占位（无新业务接口） | 关键页面文案可切换；中英文资源占位完整（G11） |

### 5.2 退出标准（DoD）
- [ ] 多住宅隔离单测 + 联调通过。
- [ ] 首页推荐接口联调，行为数据可采集。
- [ ] 暂停/恢复状态机联调，计费口径正确。
- [ ] i18n 框架接入，至少 1 个核心页中英文可切换。

### 5.3 工作量与依赖
- 悲观（单人）：US-3.1 5~7pd、US-3.2 3~4pd、US-3.3 3~4pd、US-3.4 4~6pd ≈ **15~21pd（3~4 周）**。
- 依赖：US-3.1 依赖 T-07 框架；US-3.2 依赖行为埋点数据积累（建议 v1.14 起埋点）。

---

## 六、里程碑 · M15 上线保障

> **目标**：生产可交付——流水线 + 部署 + 安全门禁 + 文档复盘。
> **对应事项**：T-09、T-10、T-02（闭环）。

| 编号 | 故事 | 责任人(角色) | 验收标准 |
|---|---|---|---|
| US-M.1 | CI/CD 流水线 + 生产 Nginx 反代部署 | 运维 | GitHub Actions 构建+单测+镜像；生产 Nginx 反代 :8082；蓝绿/滚动可行 |
| US-M.2 | 安全治理闭环（T-02） | 后端 + PM | `JWT_SECRET` 环境变量覆盖；admin 首登强制改密；安全检查清单全勾 |
| US-M.3 | 文档终版 + 项目复盘 | PM | 文档基线封版；复盘会输出改进项清单 |

- **硬性门禁**：T-02 未完成 → 不得上线。
- 工作量：US-M.1 4~6pd、US-M.2 1~2pd、US-M.3 1~2pd ≈ **6~10pd（1~2 周）**。

---

## 七、跨迭代依赖与关键路径

```
T-01 师傅端验收 ──(本周/迭代一)──► 交付完整证据
T-03 微信支付 ──(凭证)──► 阻塞于外部；到位即插迭代二首
T-04 后台深化 ──► 迭代二 US-2.2/2.3（依赖 T-04 下钻已落地）
T-02 安全治理 ──► 迭代一启动评审，M15 闭环（硬性门禁）
T-07 国际化 ──► 迭代三 US-3.1/3.4 前提
T-08 运营留存 ──► 迭代一主体，T-11 V2.0 奠基
T-05/T-06 工程化 ──► 迭代二可选，持续进行
```

**关键路径**：迭代一（运营留存 + 验收 + 安全评审） → 迭代二（支付真集成 + 报表深化） → M15（上线）。V2.0 可与 M15 部分并行（若补员）。

---

## 八、优先级与资源

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P0** | T-01 师傅端验收、T-02 安全治理、迭代一 账户账单中心 | 交付完整性与生产安全，无外部依赖 |
| **P1** | T-03 微信支付（凭证到位）、T-04 后台深化、迭代一 积分/活动/客服 | 核心商业闭环与留存 |
| **P2** | T-05 配置热刷新、T-06 压测长稳、迭代三 多住宅/推荐/暂停恢复 | 工程化与体验 |
| **P3** | T-07/T-11/G11 国际化 | 远期/多端 |

> **资源风险提示**：当前研发以 PM 单人承接为主。迭代一~三悲观总工时约 **51~70pd（≈ 10~14 周）**。建议：① 补前端 1 名（释放客户端/后台页面产能）；② 补测试 1 名（承接 T-01/T-06 验收）；③ 补运维 1 名（承接 M15）。补员后迭代可并行，总周期压缩至 6~8 周。

---

## 九、风险登记册

| 风险 | 影响 | 对策 | 状态 |
|---|---|---|---|
| 微信支付凭证长期未提供 | T-03 无法验收，支付闭环留 Mock | PM 向业务方 escalation，明确交付窗口；未到位则保留 Mock 并标注 | 阻塞（待凭证） |
| 师傅端 AppID 占位未注册 | T-01 无法提审/验收 | PM 协调注册并注入 `project.config.json` | 待办 |
| 上线前安全未治理 | 安全审计不通过 | T-02 列为 M15 硬性门禁 | 待办 |
| 人力不足（单人研发） | 排期顺延 | 评估补员或分期交付 | 待决策 |
| 数据口径不一致（账单/退款/发票） | 重复计费展示 | 统一聚合口径（V1.11 financeReport 已对齐 REFUND 单列） | 持续 |
| 本地 commit 未 push | 代码集中在沙箱 | 用户本地 push 或提供 GitHub Token | 待办 |

---

## 十、需用户拍板的决策（Open Questions）

1. **微信支付凭证**：是否可在 v1.15 窗口前提供商户号 / APIv3 / 平台证书？决定 T-03 是否排入迭代二首。
2. **补员计划**：是否补前端/测试/运维？决定迭代并行度与总周期（10~14 周 → 6~8 周）。
3. **v1.14 范围裁剪**：账户账单中心（US-1.1）是否并入 v1.14，还是单独快速补丁？建议并入，避免客户端自助体验割裂。
4. **排期节奏**：是否按「双周迭代」滚动（每双周一个可演示增量）？建议采用，便于早验收早纠偏。
5. **代码推送**：已建立 SSH 推送惯例，commit 后直接 `git push origin V1.0.1`（最近一次 a88e677 已推送）；属已解决项，后续无需额外拍板。

---

## 十一、验收总览（规划态）

| 能力 | 归属迭代 | 状态 |
|---|---|---|
| 账户账单中心 | v1.14 | 📋 规划 |
| 积分成长体系 | v1.14 | 📋 规划 |
| 优惠活动专区 | v1.14 | 📋 规划 |
| 在线客服/帮助中心 | v1.14 | 📋 规划 |
| 师傅端视觉验收 | v1.14(T-01) | 🔶 静态审计通过（真机截图待 AppID 注册+线下） |
| 安全治理评审 | v1.14 启动 / M15 闭环 | ⬜ 待办 |
| 微信支付真集成 | v1.15(T-03) | 🔶 脚手架就绪（凭证到位即插；仍阻塞于商户凭证） |
| 报表下钻标准化 | v1.15 | 📋 规划 |
| 后台扩展深化 | v1.15(T-04) | 📋 规划 |
| 配置动态刷新 | v1.15(T-05) | 📋 规划 |
| 压测长稳 | v1.15(T-06) | ✅ 基线已建立（长稳≥2h 待排期） |
| 多住宅/智能推荐/暂停恢复 | V2.0 | 🔭 规划中 |
| 国际化 i18n | V2.0(G11) | 🔭 规划中 |
| M15 上线保障 | 里程碑 | 📋 规划 |

---

© 宽带业务管理系统 · 后续项目迭代计划（V1.0）
