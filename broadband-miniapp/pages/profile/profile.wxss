/* pages/profile/profile.wxss */
.profile-hd { display: flex; align-items: center; padding: 12rpx 8rpx 28rpx; }
.profile-info { flex: 1; margin-left: 24rpx; }
.profile-name { font-size: 36rpx; font-weight: 700; }
.profile-sub { margin-top: 8rpx; font-size: 26rpx; color: var(--text-sub); }
.profile-edit { color: var(--c); font-size: 26rpx; }
