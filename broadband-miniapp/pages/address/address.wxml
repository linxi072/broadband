<view class="container">
  <view class="nav-bar">地址管理</view>
  <view class="card" wx:for="{{list}}" wx:key="id">
    <view class="row"><text>{{item.addr}}</text><text wx:if="{{item.def}}" class="pill">默认</text></view>
  </view>
  <view class="btn-primary" bindtap="add">新增地址</view>
</view>
