/* pages/move/move.wxss */
.form-item { margin: 16rpx 0; }
.form-label { font-size: 26rpx; color: var(--text-sub); }
.switch-row { padding: 20rpx 0; }
.result { margin-top: 16rpx; padding: 18rpx 22rpx; border-radius: var(--radius-sm); font-size: 26rpx; }
.result-ok { background: #f0fdf4; color: #15803d; }
.result-no { background: #fef2f2; color: #b91c1c; }
