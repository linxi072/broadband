<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.broadband.product.mapper.CustomerAdminMapper">

    <!-- 客户分群口径（与数据智能模块共用，单一来源） -->
    <sql id="segmentCase">
        CASE
          WHEN contract_days_left IS NOT NULL AND contract_days_left &lt;= 90 THEN 'RENEW'
          WHEN last_active = 0 AND order_cnt = 0 THEN 'LEAD'
          WHEN last_active = 0 OR DATEDIFF(CURDATE(), FROM_UNIXTIME(last_active/1000)) &gt; 180 THEN 'CHURN_RISK'
          WHEN level IN ('VIP','GOLD') AND DATEDIFF(CURDATE(), FROM_UNIXTIME(last_active/1000)) &lt;= 30 THEN 'HIGH_VALUE'
          WHEN DATEDIFF(CURDATE(), FROM_UNIXTIME(last_active/1000)) &lt;= 30 THEN 'GROWING'
          ELSE 'STABLE'
        END
    </sql>

    <sql id="oneCte">
        WITH cb AS (
          SELECT c.id, c.name, c.level, c.status,
            (SELECT COUNT(*) FROM biz_order b WHERE b.customer_id = c.id) AS order_cnt,
            GREATEST(
              COALESCE((SELECT MAX(created_time) FROM biz_order b WHERE b.customer_id = c.id),0),
              COALESCE((SELECT MAX(created_time) FROM points_record p WHERE p.customer_id = c.id),0),
              COALESCE((SELECT MAX(created_time) FROM support_ticket t WHERE t.customer_id = c.id),0)
            ) AS last_active,
            (SELECT MIN(DATEDIFF(ct.end_date, CURDATE())) FROM customer_contract ct
               WHERE ct.customer_id = c.id AND ct.status = 'ACTIVE') AS contract_days_left
          FROM customer c WHERE c.id = #{id}
        )
    </sql>

    <!-- 客户列表（含套餐 / 合约 / 小区 / 标签计算列） -->
    <select id="selectCustomers" resultType="java.util.LinkedHashMap">
        SELECT c.id, c.name, c.phone, c.level,
               CASE c.level WHEN 'VIP' THEN '五星' WHEN 'GOLD' THEN '四星'
                            WHEN 'SILVER' THEN '三星' ELSE '普通' END AS levelLabel,
               p.name AS pkgName,
               COALESCE(ct.monthly_fee, p.monthly_fee, 0) AS monthlyFee,
               ct.end_date AS contractEnd, c.address,
               com.name AS communityName,
               CONCAT_WS(',',
                 IF(p.monthly_fee &gt;= 199, '千兆', NULL),
                 IF(ct.id IS NOT NULL, '合约中', NULL),
                 IF(ct.end_date IS NOT NULL AND ct.end_date &lt;= DATE_ADD(CURDATE(), INTERVAL 90 DAY), '临期', NULL)
               ) AS tagStr,
               CASE WHEN ct.end_date IS NOT NULL AND ct.end_date &lt;= DATE_ADD(CURDATE(), INTERVAL 90 DAY)
                    THEN '待续约' ELSE '在用' END AS status
        FROM customer c
        LEFT JOIN package_info p ON p.id = c.package_id
        LEFT JOIN customer_contract ct ON ct.customer_id = c.id AND ct.status = 'ACTIVE'
        LEFT JOIN community com ON com.id = c.community_id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (c.name LIKE CONCAT('%',#{keyword},'%') OR c.phone LIKE CONCAT('%',#{keyword},'%'))
            </if>
        </where>
        ORDER BY c.id
    </select>

    <!-- 客户 360 档案 -->
    <select id="selectProfile" resultType="java.util.LinkedHashMap">
        SELECT c.id, c.name, c.phone, c.level,
               CASE c.level WHEN 'VIP' THEN '五星' WHEN 'GOLD' THEN '四星'
                            WHEN 'SILVER' THEN '三星' ELSE '普通' END AS levelLabel,
               p.name AS pkgName, p.monthly_fee AS pkgMonthlyFee,
               c.package_id AS packageId, c.community_id AS communityId,
               com.name AS communityName, c.address,
               CASE c.status WHEN 'ACTIVE' THEN '在用' WHEN 'SUSPENDED' THEN '暂停'
                            WHEN 'CLOSED' THEN '已销户' ELSE c.status END AS statusLabel,
               ct.id AS contractId, ct.start_date AS contractStart, ct.end_date AS contractEnd,
               CASE ct.status WHEN 'ACTIVE' THEN '生效中' WHEN 'EXPIRED' THEN '已到期'
                              WHEN 'TERMINATED' THEN '已终止' ELSE ct.status END AS contractStatus,
               ct.monthly_fee AS contractMonthlyFee,
               CONCAT_WS(',',
                 IF(p.monthly_fee &gt;= 199, '千兆', NULL),
                 IF(ct.id IS NOT NULL, '合约中', NULL),
                 IF(ct.end_date IS NOT NULL AND ct.end_date &lt;= DATE_ADD(CURDATE(), INTERVAL 90 DAY), '临期', NULL)
               ) AS tagStr
        FROM customer c
        LEFT JOIN package_info p ON p.id = c.package_id
        LEFT JOIN customer_contract ct ON ct.customer_id = c.id AND ct.status = 'ACTIVE'
        LEFT JOIN community com ON com.id = c.community_id
        WHERE c.id = #{id}
    </select>

    <!-- 业务订单 -->
    <select id="selectOrders" resultType="java.util.LinkedHashMap">
        SELECT id, package_name AS packageName, amount, order_type AS orderType,
               CASE order_type WHEN 'NEW_INSTALL' THEN '新装' WHEN 'MOVE' THEN '移机'
                                WHEN 'RENEW' THEN '续费' WHEN 'SPEED_UP' THEN '提速'
                                WHEN 'REPAIR' THEN '报修' WHEN 'ADDON' THEN '加购' ELSE order_type END AS orderTypeLabel,
               CASE status WHEN 'PENDING' THEN '待支付' WHEN 'PAID' THEN '已支付'
                          WHEN 'INSTALLING' THEN '安装中' WHEN 'DONE' THEN '已完成'
                          WHEN 'CANCELLED' THEN '已取消' ELSE status END AS statusLabel,
               DATE_FORMAT(FROM_UNIXTIME(created_time/1000), '%Y-%m-%d %H:%i') AS createdAt
        FROM biz_order WHERE customer_id = #{id} ORDER BY created_time DESC LIMIT 50
    </select>

    <!-- 合约 -->
    <select id="selectContracts" resultType="java.util.LinkedHashMap">
        SELECT id, package_id AS packageId, monthly_fee AS monthlyFee,
               start_date AS startDate, end_date AS endDate,
               CASE status WHEN 'ACTIVE' THEN '生效中' WHEN 'EXPIRED' THEN '已到期'
                           WHEN 'TERMINATED' THEN '已终止' ELSE status END AS statusLabel
        FROM customer_contract WHERE customer_id = #{id} ORDER BY start_date DESC LIMIT 20
    </select>

    <!-- 流量（最近周期） -->
    <select id="selectTraffic" resultType="java.util.LinkedHashMap">
        SELECT period_month AS period, mobile_total AS mobileTotal, mobile_used AS mobileUsed,
               broadband_hours AS broadbandHours, broadband_peak AS broadbandPeak,
               daily_trend AS dailyTrend
        FROM traffic_usage WHERE customer_id = #{id} ORDER BY period_month DESC LIMIT 1
    </select>

    <!-- 投诉与评价 -->
    <select id="selectReviews" resultType="java.util.LinkedHashMap">
        SELECT id, order_id AS orderId, score, tags, type, content,
               CASE type WHEN 'COMPLAINT' THEN '投诉' ELSE '评价' END AS typeLabel,
               CASE status WHEN 'PENDING' THEN '待处理' WHEN 'PROCESSING' THEN '处理中'
                           WHEN 'VISITED' THEN '已回访' WHEN 'CLOSED' THEN '已闭环'
                           ELSE status END AS statusLabel,
               DATE_FORMAT(FROM_UNIXTIME(created_time/1000), '%Y-%m-%d %H:%i') AS createdAt
        FROM review WHERE customer_name = #{name} ORDER BY created_time DESC LIMIT 50
    </select>

    <!-- 安装工单 -->
    <select id="selectWorkOrders" resultType="java.util.LinkedHashMap">
        SELECT wo.id, wo.status, wo.time_slot AS timeSlot, wo.customer_name AS customerName,
               wo.package_desc AS packageDesc, wo.worker_id AS workerId,
               wo.down_speed AS downSpeed, wo.up_speed AS upSpeed,
               wo.sign_name AS signName, wo.complete_time AS completeTime,
               bo.id AS bizOrderId, bo.status AS bizStatus,
               CASE wo.status WHEN 'PENDING' THEN '待派单' WHEN 'ASSIGNED' THEN '已派单'
                              WHEN 'INSTALLING' THEN '安装中' WHEN 'DONE' THEN '已完成'
                              WHEN 'CANCELLED' THEN '已取消' ELSE wo.status END AS statusLabel
        FROM work_order wo
        LEFT JOIN biz_order bo ON bo.id = wo.biz_order_id
        WHERE bo.customer_id = #{id} OR wo.customer_name = #{name}
        ORDER BY wo.complete_time DESC, wo.id DESC LIMIT 50
    </select>

    <!-- 升级申请单 -->
    <select id="selectUpgradeOrders" resultType="java.util.LinkedHashMap">
        SELECT id, from_package_id AS fromPackageId, target_band_key AS targetBandKey,
               addon_keys AS addonKeys, month_diff AS monthDiff, one_time_diff AS oneTimeDiff,
               current_fee AS currentFee, new_fee AS newFee, effect_type AS effectType,
               CASE status WHEN 'SUBMITTED' THEN '待审核' WHEN 'EFFECTIVE' THEN '已生效'
                           WHEN 'REJECTED' THEN '已驳回' ELSE status END AS statusLabel,
               DATE_FORMAT(FROM_UNIXTIME(created_time/1000), '%Y-%m-%d %H:%i') AS createdAt
        FROM package_upgrade_order WHERE customer_id = #{id} ORDER BY created_time DESC LIMIT 20
    </select>

    <!-- 汇总指标 -->
    <select id="selectSummary" resultType="java.util.LinkedHashMap">
        SELECT
          (SELECT COUNT(*) FROM biz_order WHERE customer_id = #{id}) AS orderCount,
          (SELECT COALESCE(SUM(amount),0) FROM biz_order WHERE customer_id = #{id} AND status IN ('PAID','INSTALLING','DONE')) AS paidAmount,
          (SELECT COUNT(*) FROM work_order wo LEFT JOIN biz_order bo ON bo.id = wo.biz_order_id WHERE bo.customer_id = #{id} OR wo.customer_name = #{name}) AS workOrderCount,
          (SELECT COUNT(*) FROM review WHERE customer_name = #{name}) AS reviewCount,
          (SELECT COALESCE(ROUND(AVG(score),1),0) FROM review WHERE customer_name = #{name}) AS avgScore,
          (SELECT COUNT(*) FROM package_upgrade_order WHERE customer_id = #{id}) AS upgradeCount
    </select>

    <!-- 单客户分群判定 -->
    <select id="selectCustomerSegment" resultType="java.util.LinkedHashMap">
        <include refid="oneCte"/>
        SELECT <include refid="segmentCase"/> AS segment,
               last_active AS lastActive,
               contract_days_left AS contractDaysLeft
        FROM cb
    </select>

</mapper>
